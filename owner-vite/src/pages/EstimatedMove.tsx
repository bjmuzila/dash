import Placeholder from "./Placeholder";

/** Est. Moves BE — port of app/estimated-move/page.tsx. Body lands in a later pass. */
export default function EstimatedMove() {
  return <Placeholder title="Est. Moves BE" sourceRoute="/estimated-move" />;
}
