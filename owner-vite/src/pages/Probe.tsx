import Placeholder from "./Placeholder";

/** Probe — port of app/owner/probe/page.tsx. Body lands in a later pass. */
export default function Probe() {
  return <Placeholder title="Probe" sourceRoute="/owner/probe" />;
}
