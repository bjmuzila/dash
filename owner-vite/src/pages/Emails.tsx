import Placeholder from "./Placeholder";

/** Emails — port of app/owner/admin/emails/page.tsx. Body lands in a later pass. */
export default function Emails() {
  return <Placeholder title="Emails" sourceRoute="/owner/admin/emails" />;
}
