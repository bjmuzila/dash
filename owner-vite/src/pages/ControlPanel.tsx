/**
 * RETIRED 2026-09-15 — the Owner → Overview page no longer exists.
 *
 * Its traffic half (KPI strip, traffic/signup charts, top pages, acquisition,
 * ticker visits) is pages/Customers.tsx; its system half (server tiles,
 * hosting strip, status dots, rows written today) is components/SystemHealth.tsx
 * on the Admin page. The Campaign Link Builder went to Sales.
 *
 * /owner/dev/owner redirects to /owner/customers (OWNER_REDIRECTS in lib/nav.ts).
 * This file is unreferenced and can be deleted.
 */
export {};
