/**
 * DEPRECATED — safe to delete.
 *
 * The Scanner-only sub-strip was generalised into SectionSubStrip (driven by
 * ./sectionNav) so Test Lab could use the same row. Nothing imports this file;
 * it only still exists because the tooling that wrote these changes cannot
 * delete files on disk.
 */
export { default } from "./SectionSubStrip";
