"use client";

/**
 * Deprecated. The persistent left sidebar has been removed — navigation now
 * lives in the toolbar hamburger dropdown (see NavMenu.tsx, mounted by
 * GlobalToolbar). This stub remains only so any lingering import resolves; it
 * renders nothing. Safe to delete once no imports reference it.
 */
export default function Sidebar() {
  return null;
}
