# ── ALREADY APPLIED to the repo's docker-compose.yml (service: demo-web) ──
# Kept here as a record of what was added. Nothing to paste.
  demo-web:
    build:
      context: ./demo-owner
    image: demo-web:latest
    container_name: demo-web
    restart: unless-stopped
    ports:
      # Loopback only — Cloudflare Tunnel reaches it, nothing public.
      - "127.0.0.1:8087:8087"
